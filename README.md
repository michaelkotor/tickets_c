# Let's be ready for the exam!

## Как с этим работать?

Связаться со мной (указать ник), запросить права, создать свою ветку, добавить к ней решение, создать пулл-реквекст (зеленая кнопочка), ждать!

Если используем git:

git clone "https://github.com/michaelkotor/tickets_c"

git checkout -b nameOfBranch

git push origin/nameOfBranch

Создать пулл-реквест.


## Билет 1

- Tранспонировать матрицу относительно побочной диагонали используя while не создавая новую матрицу

- В массиве строк найти большее слово и вывести его.

## Билет 2

- Строка из пробелов и групп цифр. В функции найти сумму всех чисел в строке.

- В функции перемножить две матрицы циклом do...while

## Билет 3

- Два массива вещественных чисел (при вводе должны быть отсортированы по убыванию). В ФУНКЦИИ объединить их в третий массив упорядоченный по убыванию(без сортировки). 

- Строка. В ФУНКЦИИ реверсивный переворот нечетных слов (отправлять слова по одному) 

## Билет 4

- Фибоначчи рекурсией (там на всякий случай еще через for и while)

- Реверс первого и последнего слова в строке


## Билет 5

- Даны две строки. В функции в большую строку записать меньшую с элемента n.

## Билет 6

- Рекурсивно транспонировать матрицу относительно побочной диагонали

- Рекурсивно удалить предпоследнее слово в строке(не используя новую строку)

## Билет 7

- Ввести через указатели массив и отсортировать от n до b используя метод Шелла

- Ввести динамическую строку, каждое слово рекурсивно перевернуть и вывести 

## Билет 8

-Oтсортировать строку вводимую пользователем по алфавиту не используя массивы и другие, а используя свои функции

## Билет 9

- Отсортировать n-ую строку массива матрицы длинных (long int например) чисел вставками в функции

- В функции поменять 1 и 2 слово местами

## Билет 10

- Создать функцию перевода из 10-чной в любую, в диапазоне от 2 до 16(вводится пользователем), не используем рекурсий.

- Вводятся строки. Создать функцию, в которой за один проход мы определяем, сколько раз повторяются строчные русские буквы (число встреч   / все буквы в строке)

## Билет 11

- Разработать функцию которая отдельно считает инты и флоты используя макросы

- Через командную строку. В функции удалить первое слово и такие же слова

## Билет 13

- Дана кв. Матрица коротких целых чисел, в функции отсортировать вставками главную диагональ по убыванию

- Есть строка из любых символов, найти самое большое число(не цифру)(типа овг12 рв7!567.   Ответ 567)

## Билет 15

- Рекурсией написать функции принимающую дробную часть числа, и переводящую ее в другую систему счисления номер системы счисления вводится вручную(от 2 до 16).


- В функцию передать матрицу строк(по-строково) в каждой строке найти первое слово, и если слово состоит из первых 15 строчных(малых) букв от а, вывести его(нельзя использовать дополнительных массивов и строк)

## Билет 16

- В Мэйн ввести массив из n (n с Клавы)  строк. Передать их в функцию с переменным количеством параметров, найти самое маленькое слово и вывести его на экран.

- Найти в строке самое маленькое слово и поменять местами с следующем, если его нет ничего не делать.

## Билет 17

- Не используя массивов ввести группу длинных чисел и вывести их в обратном порядке с их порядковыми номерами.

- Создать функцию сложения int и float

## Билет 18

- Отсортировать i-ую строку в матрице под побочной диагональю, используя указатели.

- Написать рекурсивную функцию, принимающую указатель на строку, для переворота последнего слова в строке

## Билет 19

- Дан массив из матриц— транспонировать каждую матрицу относительно главной диагонали.

- Ввести две строки, подсчитать их длину, переместить большую строку в конец меньшей без использования дополнительных строк(все это через функции

## Билет 20

- Проверить побочную диагональ квадратной матрицы на повторяющемся элементы и обнулить их ( использовать только цикл while).

- Передать строку в функцию в ней же удалить лишние пробелы (слова должны быть отделены одним пробелом ) вернуть строку в main().

## Билет 21

- В функцию передать матрицу длинных вещественных чисел(используя указатель), отсортировать строки по возрастанию, которые начинаются с чётного элемента. Использовать "шейкер".

- В функцию передать строку, найти самое короткое слово и поменять его с предыдущим словом. Если это первое слово, ничего не делать.

## Билет 22

- Функция с переменным числом параметров, в которой массив или что-то вроде того, чекайте в книге Луцика задачи по этой теме на массивы.

- Написать функцию, которая будет менять два слова местами. Первое слово выбирает пользователь, и нужно поменять его со след.

## Билет 24

- Создать рекурсивную функцию,которая делает реверс побочной диагонали.

- Дан массив строк(в строке находится ТОЛЬКО фамилия). Отсортировать строки в алфавитном порядке любым методом сортировки, не используя дополнительных символьных массивов.

## Билет 25

- Умножить матрицу на матрицу в функции. Их же изначально ввести.

- Дана строка в которой short signed int разделены пробелами, в функции все числа записать в массив, функция возвращает этот массив. То есть она будет int* func(char* string)

## Билет 26

- Ввести два массива целых чисел через указатели, первый по возрастанию вводится,второй по убыванию(сделать проверку при вводе). В функции сформировать третий массив по возрастанию не используя сортировок.

- Ввести строку в main . В функции удалить из строки первый элемент,и все,которые ему соответствуют. Не использовать новых строк.

## Билет 28

- Разработать рекурсию для транспонирования матрицы относительно главной диагонали.

- Дан массив строк. В функции найти максимальное слово и передать его в main.

## Билет 29

- Создать НЕрекурсивную функцию перевода ДРОБНОЙ части числа из 10 в любую из 2-16 систем счисления.

- Рекурсивная функция удаления первого слова в строке.

## Билет 30

- Вводится в мэйн 2 массива(через указатели) числа в них расположены по убыванию(нужна естественно проверка), нужно в функции совместить два массива в третий, расположив числа по возрастанию(не используя сортировку)

- Через мэйн вводятся две строки, используя функцию первую вставить во вторую, начиная с позиции н(параметр функции). дополнительных строк не использовать


## Билет 31

- Написать функцию сортировки методом Шелла по убыванию

- Рекурсивная функция удаления пробелов в строках

## Билет 32

- Найти сумму первых 5 четных чисел под главной диагональю. Рекурсией. Использовать указатель на указатель.

- Через командную строку В Мейн передать строки(каждая строка-номер года). Найти сумму високосных лет(кратных 4) в функции(строки передавать по одному). Вывести в мейн.

## Билет 33

- Ввести массив чисел, найти среднее арифметическое и найти количество и сумму всех чисел меньших или равных среднему

- Ввести массив строк через командную строку, потом рекурсией с помощью указателя найти строку в которой больше слов, начинающихся с большой русской буквы

## Билет 36

- Транспонировать матрицу относительно побочной диагонали

- Ввести несколько строк.Удалить лишние пробелы в строке, используя указатель на функцию, который указывает на строку. (как понять используя указатель на функцию, который указывает на строку?)

## Билет 37

- Используя указатель на функцию реализовать программу вычисления корня нелинейного уравнения. Метод, реализуемый в программе - половинного деления (или можно выбрать по своему усмотрению) +
- Разработать рекурсивную функцию реализующую проверку во введённой строке правильность порядка [ ](каждой [ соответствует ])([ ] [ [ ] ] - верно,[ ] ] [ ] [- неверно ). Дополнительные строки не использовать

## Билет 39

- Используя массив указателей на функцию, написать диалоговое окно

- Рекурсивно удалить второе слово в строке (функция задана параметром)(что значит функция задана параметром?) //ну мб функция это параметр в другой функции

## Билет 40

- Отсортировать Хоара целочисленный массив используя функцию. Результат на печать в main(т.е надо из функции вернуть массив   отсортированный). Все динамически.

- Все динамически. Выделить память под строки, перевернуть каждое слово(в функцию передается слова отдельно), нерекурсивно.
